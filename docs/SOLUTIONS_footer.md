if (typeof document !== 'undefined') {
    document.addEventListener('DOMContentLoaded', () => {
        // Place the original code logic here
    });
} else {
    // Optional: Add fallback or testing logic for Node.js environment if needed
    // Example: console.log("Script running outside a browser context.");
}